import { NextPage } from 'next';
import { Header } from '@octobanzo/components';

interface Props {}

export default (props: Props) => {
    return (
        <div>
            <Header />
            <h1>Hello</h1>
        </div>
    );
};
